using UnityEngine;

public class TryCatchFinallyDemo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {   /*
       int result = 0;
        
        try //예외가 발생할 가능성이 있는 구문이 들어오는 코드블록
        {
            result = x / y; //0으로 나누면 예외 발생 여기서 try절 중단 후 catch절 실행
            //[1]
            Debug.Log($"[1]:{x} / {y} = {result}");
        }
        catch //try절에서 예외가 발생하면 catch 절을 실행
        {
            //[2]
            Debug.Log("[2]: 예외가 발생했습니다");
        }
        finally //try절에서 예외가 발생하든 안하든 상관없이 무조건 실행
        {
            //[3]
            Debug.Log("[3]: finally 절을 실행합니다");
        }
        */
    }

}
